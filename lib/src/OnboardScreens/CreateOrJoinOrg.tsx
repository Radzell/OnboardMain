import React from "react"
import { Flow } from ".."

const CreateOrJoinOrg = ({ flow, onNext }: {  flow: Flow,onNext: (data?: Record<string, any>) => void }) => {
    return (
        <div>

        </div>
    )
}

export default CreateOrJoinOrg